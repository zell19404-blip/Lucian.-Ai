<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lucian.AI - Chat Interface</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body { background-color: #f3f4f6; }
        .chat-container { height: calc(100vh - 160px); }
        .message-lucian { background-color: #ffffff; border-radius: 15px 15px 15px 0px; }
        .message-user { background-color: #3b82f6; color: white; border-radius: 15px 15px 0px 15px; }
    </style>
</head>
<body class="flex flex-col h-screen">


    <!-- Header -->
    <header class="bg-blue-700 text-white p-4 shadow-lg flex justify-between items-center">
        <div>
            <h1 class="text-xl font-bold">Lucian.AI</h1>
            <p class="text-xs text-blue-200">Created by Fazela Alya Saputri</p>
        </div>
        <div class="text-sm bg-blue-600 px-3 py-1 rounded-full text-white">Online</div>
    </header>


    <!-- Chat Area -->
    <main id="chat-box" class="chat-container overflow-y-auto p-4 space-y-4 flex-grow">
        <!-- Pesan Sambutan -->
        <div class="flex justify-start">
            <div class="message-lucian p-3 max-w-xs md:max-w-md shadow-sm border border-gray-200">
                Halo! Saya <strong>Lucian.AI</strong>. Ada yang bisa saya bantu hari ini? 😊
            </div>
        </div>
    </main>


    <!-- Input Area -->
    <footer class="p-4 bg-white border-t border-gray-200">
        <form id="chat-form" class="flex gap-2 max-w-4xl mx-auto">
            <input type="text" id="user-input" placeholder="Ketik pesan atau minta kuis..." 
                class="flex-grow p-3 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500">
            <button type="submit" class="bg-blue-600 text-white px-6 py-3 rounded-lg font-semibold hover:bg-blue-700 transition">
                Kirim
            </button>
        </form>
    </footer>


    <script>
        const chatForm = document.getElementById('chat-form');
        const userInput = document.getElementById('user-input');
        const chatBox = document.getElementById('chat-box');


// GANTI INI DENGAN API KEY ANDA
    const API_KEY = "AQ.Ab8RN6K_fGnW1xSG6LAXtaUGzQIcpCkoJ6eWP0svumlr6D75Zw"; 
    const API_URL = `https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${API_KEY}`;




        chatForm.addEventListener('submit', (e) => {
            e.preventDefault();
            const message = userInput.value.trim();
            if (message === "") return;


            // Tambah pesan user ke layar
            addMessage(message, 'user');
            userInput.value = "";


            // Simulasi respon Lucian.AI
            setTimeout(() => {
                addMessage("Terima kasih! Saya Lucian.AI sedang memproses permintaan Anda...", 'lucian');
            }, 1000);
        });


        function addMessage(text, sender) {
            const div = document.createElement('div');
            div.className = sender === 'user' ? 'flex justify-end' : 'flex justify-start';
            
            const msgDiv = document.createElement('div');
            msgDiv.className = sender === 'user' ? 'message-user p-3 max-w-xs md:max-w-md shadow-md' : 'message-lucian p-3 max-w-xs md:max-w-md shadow-sm border border-gray-200';
            msgDiv.innerHTML = text;


            div.appendChild(msgDiv);
            chatBox.appendChild(div);
            
            // Scroll otomatis ke bawah
            chatBox.scrollTop = chatBox.scrollHeight;
        }
    </script>
</body>
</html>